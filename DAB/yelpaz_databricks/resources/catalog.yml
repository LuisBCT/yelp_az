resources:
  catalogs:
    yelp_batch:
      name: ${var.catalog}
      comment: 'Catalog created to explore yelp dataset '
      properties:
        purpose: 'Testing'
      grants:
        - principal: yelpaz_admins
          privileges:
            - USE_CATALOG
            - CREATE_SCHEMA